# Demo Screenshots

Add screenshots of the running Streamlit UI here as demo evidence for the
submission (assignment §5 — "Demo evidence").

Suggested shots (app runs at http://localhost:8501 via `streamlit run app.py`):

1. **`approve.png`** — evaluate `CLM-001` → ✅ Approve.
2. **`partial.png`** — evaluate `CLM-002` → 🟡 Partially Approve (shows the
   deductions table: lodging cap, alcohol, ground-transport cap).
3. **`manual_review.png`** — evaluate `CLM-004` → 🔍 Manual Review (business
   class needs VP approval).
4. **`audit_trail.png`** — expand the "🔎 Audit trail" panel to show the tools
   called and their results.

Tip: on macOS, `Cmd+Shift+4` then space-bar lets you capture a single window.
